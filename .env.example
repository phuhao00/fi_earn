# OpenBB 配置（可选，免费数据源不需要）
# OPENBB_PAT=your_openbb_personal_access_token

# Tushare（如需使用付费接口，填写 token）
# TUSHARE_TOKEN=your_tushare_token

# 缓存目录（默认 ./cache）
CACHE_DIR=./cache

# 日志级别（DEBUG / INFO / WARNING）
LOG_LEVEL=INFO
